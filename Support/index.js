

// Mảng (Array)
let productList = ["iPhone", "Macbook", "Apple Watch"]

// In ra phần tử đầu tiên của mảng
console.log(productList[0]);

// In ra số lượng phần tử của mảng
let leng = productList.length
console.log(leng);

// In ra từng phần tử của mảng
for (let i = 0; i < leng ; i++) {
    console.log(productList[i]);
}

// Đối tượng (Object)
let person1 = {
    fullname: "Khoa Điền",
    age: 14,
    address: "?",
    gender: "Male"
}

// in ra tên 
console.log(person1.fullname);


// Danh sách các đối tượng

